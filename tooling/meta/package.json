{
  "name": "@meta/orchestrator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "dev": "cargo run --",
    "test": "cargo test",
    "lint": "cargo clippy",
    "format": "cargo fmt"
  }
}
